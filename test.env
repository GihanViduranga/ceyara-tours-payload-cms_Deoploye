# Payload CMS Configuration
PAYLOAD_SECRET=your-secret-key-here-change-this-in-production
DATABASE_URI=mongodb+srv://username:password@cluster.mongodb.net/ceyara-tours?retryWrites=true&w=majority
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# Node.js Configuration
NODE_OPTIONS="--no-deprecation --no-experimental-strip-types"

# Email Configuration (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=gihanvidu123@gmail.com
SMTP_PASS=your-app-password-here

# Google Cloud Platform (GCP) Storage Configuration
# Required for storing images in GCP Bucket
GCP_BUCKET_NAME=your-gcp-bucket-name
GCP_PROJECT_ID=your-gcp-project-id
# Optional: Path to GCP service account JSON file (defaults to gcp-service-account.json in project root)
# GCP_KEY_FILE=gcp-service-account.json
